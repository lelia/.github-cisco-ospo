name: sync-org

on:
  push:
    branches: [main]
    paths:
      - org/**
  schedule:
    - cron:  '0 * * * *'
  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/lint.yml
  sync:
    uses: ./.github/workflows/peribolos.yml
    permissions: read-all
    with:
      confirm: true
